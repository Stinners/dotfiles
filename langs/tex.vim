" Set up latex environment 
let g:tex_flavor = "tex"
let g:polyglot_disabled = ['latex']
let g:vimgtex_complete_enabled = 1 

set spell spelllang=en_gb
set sidescrolloff=0

" Disable Vim's latex autoindenting because is more trouble than it's worth
set cindent         
